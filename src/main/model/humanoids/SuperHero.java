package main.model.humanoids;

import main.model.animals.Animal;

public class SuperHero extends Humanoid{

    @Override
    public void getNewPet(Animal animal) {
        super.getNewPet(animal);
    }
}
